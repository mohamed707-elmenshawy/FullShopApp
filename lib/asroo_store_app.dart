import 'package:flutter/material.dart';

class TestEnvType extends StatelessWidget {
  const TestEnvType({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Text('data'),
    );
  }
}
